#!/usr/bin/env node
/**
 * ADM TUI — Full-screen terminal UI
 * Replaces Commander.js CLI with ink-based TUI for v2.
 */
'use strict';

const { boot } = require('../src/tui/app');

boot().catch(err => {
  console.error(err.message);
  process.exit(1);
});
